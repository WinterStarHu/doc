# DBA_SCHEDULER_JOB_DESTS

`DBA_SCHEDULER_JOB_DESTS` displays information about the state of all jobs in the database at each of their 目标. 其列与 `ALL_SCHEDULER_JOB_DESTS` 中的列相同。
参见：
- "ALL_SCHEDULER_JOB_DESTS"
